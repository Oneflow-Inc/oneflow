name: Check PR

on:
  pull_request:
    types: [opened, labeled, unlabeled, synchronize]

jobs:
  check_labels:
    runs-on: ubuntu-18.04
    name: Labels
    steps:
      - name: Check type labels 'bug, enhancement, purge, feature, documentation'
        if: (contains(github.event.issue.pull_request.*.name, 'bug') || contains(github.event.issue.pull_request.*.name, 'enhancement') || contains(github.event.issue.pull_request.*.name, 'purge') || contains(github.event.issue.pull_request.*.name, 'feature') || contains(github.event.issue.pull_request.*.name, 'documentation')) == false
        run: |
          exit 1
      - name: Check component labels 'op, system, eager, build, xla, python, ci, test, tooling, onnx'
        if: (contains(github.event.issue.pull_request.*.name, 'op') || contains(github.event.issue.pull_request.*.name, 'system') || contains(github.event.issue.pull_request.*.name, 'eager') || contains(github.event.issue.pull_request.*.name, 'build') || contains(github.event.issue.pull_request.*.name, 'xla') || contains(github.event.issue.pull_request.*.name, 'python') || contains(github.event.issue.pull_request.*.name, 'ci') || contains(github.event.issue.pull_request.*.name, 'test') || contains(github.event.issue.pull_request.*.name, 'tooling') || contains(github.event.issue.pull_request.*.name, 'onnx')) == false
        run: |
          exit 2
