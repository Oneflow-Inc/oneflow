syntax = "proto3";

package oneflow;

import "distributed_runtime/machine_list.proto";

service InitService {
  rpc ExchangeMachineInfo(Node) returns (Node);
  rpc ExchangeMemoryDesc(MemDesc) returns (MemDesc);
}

message MemDesc {
  int32 md = 1;
}
