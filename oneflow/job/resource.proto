syntax = "proto3";
package oneflow;

message DeviceVec {
  repeated int32 device_id_vec = 1;
}

message Machine {
  required string addr = 1; // domain name or ip
  required string port = 2;
  optional DeviceVec device_vec = 3;
}

message Resource {
  repeated Machine machine_vec = 1;
}
