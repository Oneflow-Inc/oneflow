syntax = "proto3";
package oneflow;

message Machine {
  string addr = 1; // domain name or ip
  string port = 2;
}

message Resource {
  repeated Machine machine_vec = 1;
  int32 device_num_per_machine = 2;
}
