syntax = "proto2";
package oneflow;

import "oneflow/core/job/task.proto";

message OneMachineBufInfo {
  repeated uint64 buf_size = 1;
}

message Plan {
  repeated TaskProto task = 1;
  required int64 total_mbn_num = 2;
  repeated OneMachineBufInfo buf_info = 3;
}
