set -ex
docker exec \
    -e CTEST_RESOURCE_GROUP_COUNT=${CTEST_RESOURCE_GROUP_COUNT} \
    -e CTEST_RESOURCE_GROUP_0_VRAM=${CTEST_RESOURCE_GROUP_0_VRAM} \
    -e ONEFLOW_TEST_DEVICE_NUM=${ONEFLOW_TEST_DEVICE_NUM} "$@"
