# oneflow docker image

## binary ignore

```
bin
├── hadoop-2.8.1.tar.gz
└── oneflow
```

## TODO

* [ ] `HADOOP_BACKEND` env shall with `PORT` number or `PROTOCAL`

## Notice

* `web portal` conf is ignored in hadoop conf by now.%
