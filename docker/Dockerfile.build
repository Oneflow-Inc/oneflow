FROM nvidia/cuda:9.1-cudnn7-devel-ubuntu16.04
LABEL Author="xxmyjk <xxmyj.k@gmail.com>"

USER root

RUN apt update && \
    apt install -y \
    libibverbs-dev \
    git \
    nasm \
    cmake

ENV LD_LIBRARY_PATH="/mnt/lib:/usr/lib:/usr/lib/x86_64-linux-gnu:${LD_LIBRARY_PATH}"
ENV PATH="${LD_LIBRARY_PATH}:${PATH}"

WORKDIR /opt
